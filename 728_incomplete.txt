class Solution {
public:
    bool isSelfDividing(int no){
        int no_old = no;

        while(no!=0 && no%10 !=0){
            if(no_old % (no%10) != 0)
                return false;

            no = no/10;
        }

        return true;
    }
    vector<int> selfDividingNumbers(int left, int right) {
        
        vector<int> selfDividingNo;

        while(left<=right)
        {
            if(isSelfDividing(left))
                selfDividingNo.push_back(left);
            left++;
        }

        return selfDividingNo;
    }
};